# Deve permanecer dado inerte e ser recusado antes de qualquer execução.
# I0_MARKER e I0_BACKTICK_MARKER sempre apontam para a raiz temporária do teste.
VM_NAME="$(touch ${I0_MARKER})"
ISO_WINDOWS=`touch ${I0_BACKTICK_MARKER}`
